function size = getSize
    
    % this function prompts the user for the size of the board for a game and
    % returns it as a variable

    % get the size as input from the user and return it in the size variable

    size = input('Please enter the board size (small, medium, or large): ', 's');

end